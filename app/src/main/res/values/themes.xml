<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NextCourse" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">#B3000000</item>
        <item name="android:popupMenuStyle">@style/PopupMenuStyle</item>
    </style>
    <style name="PopupMenuStyle" parent="@android:style/Widget.PopupMenu">
        <item name="android:popupBackground">@drawable/menu_bg</item>
        <item name="android:dropDownHorizontalOffset">16dp</item>
    </style>
    <style name="Theme.NextCourse" parent="Base.Theme.NextCourse" />
</resources>